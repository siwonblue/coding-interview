function solution(friends, gifts) {
  const send = new Map();
  const receive = new Map();
  for (let i = 0; i < friends.length; i++) send.set(friends[i], 0);
  for (let i = 0; i < friends.length; i++) receive.set(friends[i], 0);

  for (let i = 0; i < gifts.length; i++) {
    const gift = gifts[i].split(" ");
    gift[0];
  }
}
solution(["A", "B", "C", "D"]);
